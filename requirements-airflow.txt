# Imported directly by the DAG's PythonOperator tasks, so these install into
# Airflow's environment. Pinned to the versions used locally.
#
# pandas is deliberately NOT pinned here: Airflow 2.8.0 ships its own version,
# and prophet only needs pandas >= 1.0.4. Forcing the local pandas would fight
# Airflow's constraints for no benefit.
openai==2.30.0
boto3==1.42.79
python-dotenv==1.2.2
prophet==1.3.0
